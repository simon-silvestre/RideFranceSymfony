<a{% for attr, value  in options | merge({class: 'btn translucide-bouton'}) %} {{ attr }}="{{ value }}"{% endfor %}>
    <span class="float-right">
        {% if sorted %}
            {% if direction == 'desc' %}
                <i class="ml-1 fa fa-sort-down"></i>
            {% else %}
                <i class="ml-1 fa fa-sort-up"></i>
            {% endif %}
        {% else %}
            <i class="ml-1 fa fa-sort"></i>
        {% endif %}
    </span>
    {{ title }}
</a>
